/**
 * @jest-environment jsdom
 */

require('../mocks/fetchSimulator');
const { fetchQuote } = require('../helpers/fetchQuote');
const quote = require('../mocks/quote');

describe('Teste a função fetchQuote', () => {
  // implemente seus testes aqui
  fail('Teste vazio');
});